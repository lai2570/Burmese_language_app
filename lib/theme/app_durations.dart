class AppDurations {
  static const buttonPress    = Duration(milliseconds: 50);
  static const buttonRelease  = Duration(milliseconds: 150);
  static const answerReveal   = Duration(milliseconds: 200);
  static const pageTransition = Duration(milliseconds: 300);
  static const shortDelay     = Duration(milliseconds: 500);
  static const listAnimation  = Duration(milliseconds: 800);
  static const splashDisplay  = Duration(milliseconds: 2000);
  static const quizTimer      = Duration(seconds: 5);
}
