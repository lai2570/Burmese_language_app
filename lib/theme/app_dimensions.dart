import 'package:flutter/material.dart';

class AppDimensions {
  // Border radii (use with BorderRadius.circular())
  static const double radiusXs   = 8;
  static const double radiusSm   = 12;
  static const double radiusMd   = 16;
  static const double radiusLg   = 20;
  static const double radiusXl   = 24;
  static const double radiusPill = 30;

  // Standard EdgeInsets
  static const EdgeInsets paddingXs = EdgeInsets.all(8);
  static const EdgeInsets paddingSm = EdgeInsets.all(12);
  static const EdgeInsets paddingMd = EdgeInsets.all(16);
  static const EdgeInsets paddingLg = EdgeInsets.all(20);
  static const EdgeInsets paddingXl = EdgeInsets.all(24);

  // Button paddings
  static const EdgeInsets buttonPadding   = EdgeInsets.symmetric(horizontal: 16, vertical: 12);
  static const EdgeInsets buttonPaddingV  = EdgeInsets.symmetric(vertical: 16);
  static const EdgeInsets buttonPaddingVs = EdgeInsets.symmetric(vertical: 12);
}
